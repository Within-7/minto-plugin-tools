# HTML Presentation Review Integration Guide

## 概述

`html-presentation-beautifier` 插件现已集成**增强版自动审查功能**。在生成 HTML 幻灯片后，系统会自动使用 `html-presentation-reviewer` agent 进行**严格的质量检查**。

## ⭐ 核心原则

### 设计风格严格匹配 ⚠️ CRITICAL
- **颜色必须精确匹配 McKinsey 标准**（精确到 hex 值）
- **字体大小必须符合规范**（精确到像素范围）
- **布局必须一致**（页边距、间距精确到像素）
- **所有幻灯片使用统一样式**

### 内容 100% 保留 ⚠️ CRITICAL
- **绝不精简** - 不使用"等"字省略
- **绝不总结** - 不使用"主要"、"核心"概括
- **绝不删减** - 所有章节、数据点、结论必须完整
- **保持精度** - 数据精度与源文档完全一致

### 只优化展示 ⚠️ IMPORTANT
- **只改进视觉呈现**
- **不修改内容本身**
- **不改变原文措辞**

## 新增功能（增强版）

### Phase 5: Review & Verify（强化版）

这是新增的第 5 阶段，在生成 HTML 后自动触发：

1. **McKinsey 设计风格严格验证** ⭐ MOST IMPORTANT
   - 颜色精确匹配（8 个标准颜色 hex 值逐个验证）
   - 字体大小精确（标题 48-64px, 副标题 28-36px, 正文 16-20px）
   - 布局精确（页边距 40-60px, 间距 20-30px）
   - 样式一致性（所有幻灯片统一）

2. **内容 100% 完整性验证** ⭐ CRITICAL
   - 精简模式自动检测（"等"、"主要"等关键词）
   - 数据点完整性（数量、值、精度）
   - 文本完整性（列表项、结论）
   - 原文准确度（无改写）

3. **代码质量检查**
   - HTML/CSS/JS 语法验证
   - 无控制台错误
   - 无未定义变量

4. **图表样式严格验证**
   - Chart.js 图表必须使用 McKinsey 颜色
   - 图表字体必须符合规范
   - 概念图表必须使用标准颜色和字体

5. **交互功能测试**
   - 导航、全屏、响应式测试

## 工作流程

### 完整的 5 阶段流程

```
Phase 1: Parse Document
   ↓
Phase 2: Plan Slides (subagent: general-purpose)
   ↓
Phase 3: Apply Design
   ↓
Phase 4: Generate HTML (subagent: general-purpose)
   ↓
Phase 5: Review & Verify (agent: html-presentation-reviewer) ← NEW!
   ↓
Review Report Output
```

### Phase 5 详细流程

```javascript
// 1. Phase 4 完成，HTML 文件已生成
const htmlFile = "presentation.html";

// 2. 自动调用 html-presentation-reviewer agent
const reviewResult = await reviewAgent({
  htmlFile: htmlFile,
  sourceDocument: sourceFile,
  checks: [
    "content_integrity",
    "code_quality",
    "mckinsey_style_compliance",
    "chart_validity",
    "interactivity"
  ]
});

// 3. 获取审查报告
console.log(reviewResult);
/*
{
  review_summary: {
    overall_score: 92,
    status: "PASS",
    total_issues: 3,
    critical_issues: 0,
    major_issues: 1,
    minor_issues: 2
  },
  content_integrity: { score: 100 },
  code_quality: { score: 95 },
  mckinsey_style_compliance: { score: 88 },
  chart_validity: { score: 90 },
  interactivity: { score: 100 },
  detailed_issues: [...],
  recommendations: [...]
}
*/
```

## 审查维度详解

### 1. 内容完整性 (CRITICAL)

**检查内容：**
- ✓ 所有章节已包含
- ✓ 所有数据点已可视化
- ✓ 所有结论已展示
- ✓ 无内容压缩或总结
- ✓ 原文准确无误（无改写）
- ✓ 列表项数量一致

**失败标准：**
- 任何章节缺失 → CRITICAL
- 数据点丢失 → CRITICAL
- 结论被省略 → CRITICAL
- 列表项数量不一致 → CRITICAL
- 文本被改写（非原文） → CRITICAL

### 2. 代码质量

**检查内容：**
- HTML 结构有效性
- CSS 语法正确性
- JavaScript 功能完整性
- 无控制台错误

**常见问题检测：**
```javascript
const commonIssues = [
  "Uncaught SyntaxError: missing ) after argument list",
  "Uncaught ReferenceError: navigate is not defined",
  "Uncaught TypeError: Cannot read property",
  "Missing closing tags",
  "Undefined variables in onclick handlers"
];
```

### 3. McKinsey 样式合规

**检查内容：**
- ✓ 颜色方案符合标准
- ✓ 排版层次一致
- ✓ 布局规范统一
- ✓ 专业水准评估

**标准颜色：**
- 主背景 `#FFFFFF`
- 标题栏 `#000000`
- 强调色 `#F85d42`
- 辅助色 `#74788d`
- 深蓝 `#556EE6`
- 绿色 `#34c38f`
- 蓝色 `#50a5f1`
- 黄色 `#f1b44c`

### 4. 图表正确性

**检查内容：**
- ✓ 图表类型匹配数据
- ✓ 数据可视化准确
- ✓ 概念图表合理
- ✓ 交互功能正常

### 5. 交互功能验证

**检查内容：**
- ✓ 导航按钮工作
- ✓ 键盘快捷键可用
- ✓ 全屏模式工作
- ✓ 图表交互流畅
- ✓ 响应式适配

## 审查报告示例

### 输出示例

```
✓ HTML Presentation Review Report

Overall Score: 92/100 - PASS

Content Integrity:     ✓ 100/100 (PASS) - 所有内容完整保留
Code Quality:          ✓  95/100 (PASS) - 代码质量良好
McKinsey Style:        ✓  88/100 (PASS) - 样式基本合规
Chart Validity:        ✓  90/100 (PASS) - 图表准确有效
Interactivity:         ✓ 100/100 (PASS) - 交互功能完善

Issues Found: 3
  Critical: 0
  Major: 1
  Minor: 2

Top Issues:
1. [MAJOR] 第 7 页建议使用 pyramid 图表而非列表
2. [MINOR] 部分幻灯片边距不一致
3. [MINOR] 建议添加 aria 标签以提升可访问性

Recommendations:
- 修复第 7 页图表选择
- 统一所有幻灯片边距
- 可选：添加可访问性增强

Status: ✓ APPROVED for use (with optional improvements)
```

### JSON 报告格式

```json
{
  "review_summary": {
    "overall_score": 92,
    "status": "PASS",
    "total_issues": 3,
    "critical_issues": 0,
    "major_issues": 1,
    "minor_issues": 2
  },
  "content_integrity": {
    "score": 100,
    "status": "PASS",
    "checks": {
      "sections_complete": true,
      "data_points_complete": true,
      "conclusions_complete": true,
      "exact_text_preserved": true,
      "no_content_loss": true
    }
  },
  "detailed_issues": [
    {
      "severity": "major",
      "category": "chart-selection",
      "description": "第 7 页建议使用 pyramid 图表而非列表",
      "suggestion": "内容为层级型观点，pyramid 图表更合适"
    }
  ]
}
```

## 质量评分标准

- **95-100**: ✓ 优秀，可直接使用
- **85-94**: ✓ 良好，建议小优化
- **75-84**: ⚠ 一般，有需要改进之处
- **65-74**: ❌ 需要改进，存在问题
- **<65**: ❌ 不合格，必须重新生成

## 问题严重程度

**Critical (严重)** - 必须修复，否则不合格：
- 内容缺失或丢失
- 代码语法错误导致无法运行
- JavaScript 运行时错误
- 关键功能失效

**Major (重要)** - 强烈建议修复：
- 图表类型选择不当
- 样式明显不一致
- 交互功能部分失效

**Minor (次要)** - 可选修复：
- 小的样式不一致
- 可访问性建议
- 代码优化建议

## 使用示例

### 自动审查（推荐）

```bash
# 使用 /beauty 命令，自动触发完整流程
/beauty document.md

# 自动执行：
# Phase 1-4: 生成 HTML
# Phase 5: 自动审查并输出报告
```

### 手动触发审查

如果你已有 HTML 文件，可以手动触发审查：

```bash
# 调用 html-presentation-reviewer agent
# 输入：HTML 文件 + 源文档
# 输出：详细审查报告
```

## 常见问题解答

### Q: Phase 5 会增加多少时间？

A: 通常 10-30 秒，取决于 HTML 文件大小和复杂度。

### Q: 如果审查不通过怎么办？

A:
- **Score <75**: 系统会建议重新生成
- **Critical issues**: 必须修复
- **Major issues**: 强烈建议修复
- **Minor issues**: 可选优化

### Q: 可以跳过 Phase 5 吗？

A: 不建议。Phase 5 确保内容完整性和质量，是输出前的关键检查。

### Q: 审查报告保存在哪里？

A: 审查报告会在执行完成后直接输出到控制台，便于查看。

### Q: 如何处理审查发现的问题？

A: 根据报告中的 `recommendations` 和 `fix_priority`：
1. 优先修复 Critical 和 Major 问题
2. 可选实施 Minor 改进
3. 重新运行审查验证修复效果

## 最佳实践

1. **始终使用 Phase 5** - 生成后立即审查，确保质量
2. **关注 Critical 问题** - 必须修复才能交付
3. **优先处理 Major 问题** - 影响用户体验
4. **记录审查历史** - 追踪质量改进趋势
5. **定期优化** - 根据 Minor 建议持续改进

## 技术实现

### Agent 文件位置

```
html-presentation-beautifier/
├── agents/
│   └── html-presentation-reviewer.md  ← 审查 agent 定义
├── skills/
│   └── beauty-html/
│       └── SKILL.md  ← 已更新，包含 Phase 5
└── plugin.json  ← 已注册新 agent
```

### 调用方式

在 SKILL.md 的 Phase 4 完成后：

```markdown
## Phase 5: Review & Verify (Using Subagent) - CRITICAL

**Goal**: Automatically review generated HTML presentation

**Subagent**: html-presentation-reviewer

**Input**: Generated HTML + source document

**Output**: Detailed review report
```

## 更新日志

### Version 1.1.0 (2025-01-25)

**New Features:**
- ✨ 新增 Phase 5: Review & Verify
- ✨ 集成 html-presentation-reviewer agent
- ✨ 自动内容完整性检查（100% 保留验证）
- ✨ 代码质量自动检查
- ✨ McKinsey 样式合规验证
- ✨ 交互功能自动测试

**Improvements:**
- 📈 提升输出质量和可靠性
- 📈 减少人工检查工作量
- 📈 标准化质量评估

## 反馈与贡献

如有问题或建议，请：
- 提交 issue 到项目仓库
- 联系插件维护者
- 参与 discussion 讨论

---

**注意**: Phase 5 是自动流程的一部分，确保输出质量和内容完整性。建议始终等待审查完成后再使用生成的 HTML 文件。
