# HTML Presentation Beautifier Plugin 发布指南

将插件发布到全局 Claude Code 配置，使其在所有项目中可用。

## 📋 目录

- [快速开始](#快速开始)
- [发布模式](#发布模式)
- [详细步骤](#详细步骤)
- [验证插件](#验证插件)
- [最佳实践](#最佳实践)
- [故障排除](#故障排除)
- [卸载插件](#卸载插件)

---

## 🚀 快速开始

### 方法一：复制模式（推荐用于生产环境）

```bash
cd /Users/wxj/000plugin/minto-plugin-tools/html-presentation-beautifier
./publish.sh copy
```

### 方法二：链接模式（推荐用于开发）

```bash
cd /Users/wxj/000plugin/minto-plugin-tools/html-presentation-beautifier
./publish.sh link
```

### 验证发布

```bash
./validate-plugin.sh
```

### 在 Claude Code 中使用

```bash
# 重启 Claude Code 或重新加载插件
/reload-plugins

# 查看帮助
/help

# 测试命令
/beauty --help
```

---

## 📦 发布模式

### 复制模式 (copy)

**特点**:
- 将插件完整复制到全局目录
- 独立于源代码，修改源代码不会影响已发布的插件
- 适合生产环境使用

**优点**:
- 稳定可靠
- 不受源代码修改影响
- 适合版本管理

**缺点**:
- 占用更多磁盘空间
- 更新需要重新发布

**使用场景**:
- 生产环境部署
- 分发给其他用户
- 版本发布

### 链接模式 (link)

**特点**:
- 创建符号链接到源代码目录
- 源代码修改立即生效
- 适合开发和调试

**优点**:
- 节省磁盘空间
- 源代码修改即时生效
- 方便开发和调试

**缺点**:
- 依赖源代码目录
- 不适合生产环境

**使用场景**:
- 开发和调试
- 快速迭代
- 本地测试

---

## 📝 详细步骤

### 步骤 1: 验证插件结构

在发布前，先验证插件结构是否正确：

```bash
./validate-plugin.sh
```

确保所有检查都通过。

### 步骤 2: 选择发布模式

根据你的需求选择合适的发布模式：

- **开发阶段**: 使用 `link` 模式
- **生产环境**: 使用 `copy` 模式

### 步骤 3: 执行发布

#### 复制模式

```bash
./publish.sh copy
```

输出示例：
```
🚀 HTML Presentation Beautifier Plugin 发布工具
================================================

📦 插件信息:
   名称: html-presentation-beautifier
   源目录: /Users/wxj/000plugin/minto-plugin-tools/html-presentation-beautifier
   目标目录: /Users/wxj/.claude/plugins/html-presentation-beautifier

📋 发布模式: 复制 (copy)

⏳ 开始发布...
📦 正在复制插件文件...
✅ 已清理开发文件

🔍 验证发布结果...
✅ JSON 格式验证通过

✅ 发布成功！

📁 插件已安装到: /Users/wxj/.claude/plugins/html-presentation-beautifier

📋 下一步操作:
   1. 重启 Claude Code 或运行: /reload-plugins
   2. 验证插件: /help
   3. 测试命令: /beauty --help
```

#### 链接模式

```bash
./publish.sh link
```

输出示例：
```
🚀 HTML Presentation Beautifier Plugin 发布工具
================================================

📦 插件信息:
   名称: html-presentation-beautifier
   源目录: /Users/wxj/000plugin/minto-plugin-tools/html-presentation-beautifier
   目标目录: /Users/wxj/.claude/plugins/html-presentation-beautifier

🔗 发布模式: 符号链接 (link)

⏳ 开始发布...
🔗 正在创建符号链接...

🔍 验证发布结果...
✅ JSON 格式验证通过

✅ 发布成功！

📁 插件已安装到: /Users/wxj/.claude/plugins/html-presentation-beautifier
```

### 步骤 4: 重新加载插件

在 Claude Code 中重新加载插件：

```bash
/reload-plugins
```

或者重启 Claude Code。

### 步骤 5: 验证插件

查看插件是否正确加载：

```bash
/help
```

你应该能看到 `/beauty` 命令在帮助列表中。

### 步骤 6: 测试功能

测试插件功能是否正常：

```bash
/beauty --help
```

或者转换一个测试文档：

```bash
/beauty test_document.md
```

---

## ✅ 验证插件

### 使用验证脚本

```bash
./validate-plugin.sh
```

### 查看详细验证信息

```bash
./validate-plugin.sh verbose
```

### 验证输出示例

```
🔍 HTML Presentation Beautifier Plugin 验证工具
================================================

📁 检查源目录结构
----------------------------------------
✅ 插件源目录存在
✅ .claude-plugin 目录存在
✅ commands 目录存在
✅ agents 目录存在
✅ skills 目录存在

📝 检查配置文件
----------------------------------------
✅ plugin.json 存在
✅ .claude-plugin/plugin.json 存在
✅ README.md 存在
✅ CHANGELOG.md 存在

⚡ 检查命令文件
----------------------------------------
✅ beauty.md 命令文件存在

🤖 检查代理文件
----------------------------------------
✅ presentation-merger.md 代理存在
✅ content-merger.md 代理存在
✅ visualization-optimizer.md 代理存在
✅ content-reviewer.md 代理存在
✅ html-presentation-reviewer.md 代理存在

🎯 检查技能文件
----------------------------------------
✅ beauty-html/SKILL.md 存在
✅ beauty-html/assets 目录存在
✅ beauty-html/templates 目录存在
✅ beauty-html/references 目录存在

🔧 验证 JSON 格式
----------------------------------------
✅ plugin.json JSON 格式有效
✅ .claude-plugin/plugin.json JSON 格式有效

🌐 检查全局插件目录
----------------------------------------
✅ 全局插件目录存在
✅ 插件已发布到全局目录

✨ 验证已发布的插件
----------------------------------------
✅ 全局 plugin.json 存在
✅ 全局 .claude-plugin/plugin.json 存在
✅ 全局 beauty.md 命令存在
✅ 全局 beauty-html 技能存在
✅ 全局 plugin.json JSON 格式有效
✅ 全局 .claude-plugin/plugin.json JSON 格式有效

📊 检查插件元数据
----------------------------------------
✅ 版本: 1.0.0
✅ 描述: 将文档、数据、结论等信息转化为通俗连贯...

🔐 检查脚本权限
----------------------------------------
✅ publish.sh 可执行
✅ validate-plugin.sh 可执行
✅ install.sh 可执行

========================================
📊 验证总结
========================================
总检查数: 28
通过: 28
失败: 0

🎉 所有检查通过！插件配置正确。

✅ 插件已发布到全局目录

📋 下一步:
   1. 重启 Claude Code 或运行: /reload-plugins
   2. 验证插件: /help
   3. 测试命令: /beauty --help
```

---

## 💡 最佳实践

### 1. 开发流程

```bash
# 1. 使用链接模式进行开发
./publish.sh link

# 2. 修改代码后立即生效

# 3. 验证修改
./validate-plugin.sh

# 4. 在 Claude Code 中测试
/reload-plugins
/beauty test.md
```

### 2. 发布流程

```bash
# 1. 验证插件
./validate-plugin.sh

# 2. 更新版本号
# 编辑 plugin.json 和 .claude-plugin/plugin.json

# 3. 提交代码
git add .
git commit -m "Release v1.0.0"
git tag v1.0.0
git push origin main --tags

# 4. 发布到全局
./publish.sh copy

# 5. 验证发布
./validate-plugin.sh
```

### 3. 版本管理

- 使用语义化版本号 (Semantic Versioning)
- 在 `CHANGELOG.md` 中记录变更
- 为每个发布版本打 Git 标签

### 4. 文档维护

- 保持 `README.md` 更新
- 在 `CHANGELOG.md` 中记录所有变更
- 为新功能添加使用示例

### 5. 测试

- 发布前运行验证脚本
- 在 Claude Code 中测试所有功能
- 使用不同的文档类型进行测试

---

## 🔧 故障排除

### 问题 1: 插件未加载

**症状**: 运行 `/beauty` 提示命令未找到

**解决方案**:
```bash
# 1. 检查插件目录
ls -la ~/.claude/plugins/html-presentation-beautifier/

# 2. 验证插件
./validate-plugin.sh

# 3. 重新加载插件
/reload-plugins

# 4. 重启 Claude Code
```

### 问题 2: JSON 格式错误

**症状**: 验证脚本提示 JSON 格式无效

**解决方案**:
```bash
# 1. 验证 JSON 格式
python3 -m json.tool plugin.json
python3 -m json.tool .claude-plugin/plugin.json

# 2. 修复 JSON 错误

# 3. 重新验证
./validate-plugin.sh
```

### 问题 3: 权限错误

**症状**: 脚本无法执行

**解决方案**:
```bash
# 添加执行权限
chmod +x publish.sh
chmod +x validate-plugin.sh
chmod +x install.sh
```

### 问题 4: 链接失效

**症状**: 使用链接模式后插件无法使用

**解决方案**:
```bash
# 1. 检查链接
ls -la ~/.claude/plugins/html-presentation-beautifier/

# 2. 如果链接失效，重新创建
rm -rf ~/.claude/plugins/html-presentation-beautifier
./publish.sh link
```

### 问题 5: 旧版本残留

**症状**: 更新后仍使用旧版本

**解决方案**:
```bash
# 1. 删除旧版本
rm -rf ~/.claude/plugins/html-presentation-beautifier

# 2. 重新发布
./publish.sh copy

# 3. 重新加载
/reload-plugins
```

---

## 🗑️ 卸载插件

### 完全卸载

```bash
# 1. 删除插件目录
rm -rf ~/.claude/plugins/html-presentation-beautifier

# 2. 重新加载插件
/reload-plugins
```

### 验证卸载

```bash
# 检查插件是否已删除
ls -la ~/.claude/plugins/

# 在 Claude Code 中验证
/help
```

---

## 📚 相关文档

- [README.md](README.md) - 插件概述
- [PLUGIN_GUIDE.md](PLUGIN_GUIDE.md) - 插件使用指南
- [CHANGELOG.md](CHANGELOG.md) - 变更日志
- [PLUGIN_INSTALL.md](PLUGIN_INSTALL.md) - 安装指南

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📄 许可证

MIT License

---

**最后更新**: 2026-01-28
